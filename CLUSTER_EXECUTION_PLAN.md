# Cluster Execution Plan

Purpose: prioritize growth by real product support, monetization relevance, and durable search opportunity. This plan does not authorize unsupported routes.

Priority logic:
1. Existing strongest live routes first.
2. Highest-intent clusters second.
3. High-volume evergreen clusters third.
4. New clusters only when product support exists.

## 1. PDF workflows

Why this cluster matters:
This is the strongest live inventory: 95+ PDF tools already exist across locale-prefixed routes, with monetizable download intent and browser-first privacy positioning.

Expected traffic ceiling:
2M-4M monthly views when every major PDF task has a tool, hub, troubleshooting, comparison, and workflow layer.

Monetization alignment:
High. Users complete file tasks, hit result/download surfaces, and can see sponsor previews without degrading the tool.

Top 20 seed pages:
1. compress-pdf
2. merge-pdf
3. split-pdf
4. organize-pdf
5. jpg-to-pdf
6. pdf-to-jpg
7. edit-pdf
8. sign-pdf
9. encrypt-pdf
10. decrypt-pdf
11. ocr-pdf
12. crop-pdf
13. rotate-pdf
14. remove-metadata
15. sanitize-pdf
16. repair-pdf
17. flatten-pdf
18. pdf-to-docx
19. word-to-pdf
20. extract-pages

Required tool pages:
- Build or upgrade only the tools that can complete the task in the browser.
- Preserve client-side privacy messaging and clear input/output constraints.
- Add examples, edge cases, and adjacent-tool links to every important tool page.

Required support pages:
- Use-case pages for common jobs.
- Troubleshooting pages for failure modes.
- Spec/reference pages only when requirements are maintained.

Required comparison pages:
- Compare adjacent tools with explicit job boundaries.
- Explain when not to use the current tool.
- Link to both alternatives with descriptive anchors.

Internal-link plan:
- Hub to top tools, top support pages, and workflows.
- Tool to hub, adjacent tools, troubleshooting, comparisons, and workflow.
- Support page back to exact tool, hub, and next likely action.
- Cross-cluster links only when the workflow is real.

Schema plan:
- SoftwareApplication for live tools.
- BreadcrumbList for all indexable pages.
- ItemList for hubs and collections.
- FAQ/HowTo only when the visible content actually contains that structure.

Image/media plan:
- Use media only when it clarifies the task or improves preview CTR.
- Descriptive filenames and alt text.
- No decorative image inflation.

Content freshness and maintenance:
- Review query data monthly once Search Console has impressions.
- Review specs quarterly, or sooner when a platform requirement changes.
- Mark pages for rewrite if they lose traffic while the matching tool remains healthy.

## 2. Exact-KB image and upload-limit fixes

Why this cluster matters:
Exact upload-limit searches have urgent intent, clear success criteria, and strong long-tail breadth without needing city or demographic doorway pages.

Expected traffic ceiling:
1M-2M monthly views after image compression, resizing, crop, format conversion, and portal upload troubleshooting tools exist.

Monetization alignment:
High. The user has a task failure and is likely to accept a labeled partner next step after completion.

Top 20 seed pages:
1. compress-image-to-20kb
2. compress-image-to-50kb
3. compress-image-to-100kb
4. resize-image-for-upload
5. signature-image-too-large
6. photo-too-large-for-form
7. profile-photo-upload-failed
8. image-upload-limit-fixer
9. convert-photo-to-jpg
10. reduce-photo-file-size
11. make-image-under-200kb
12. compress-png-under-100kb
13. compress-jpg-under-50kb
14. image-dimensions-checker
15. photo-rejected-by-portal
16. application-form-photo-resize
17. school-form-photo-upload
18. job-application-photo-upload
19. image-too-large-for-email
20. scan-photo-size-fixer

Required tool pages:
- Build or upgrade only the tools that can complete the task in the browser.
- Preserve client-side privacy messaging and clear input/output constraints.
- Add examples, edge cases, and adjacent-tool links to every important tool page.

Required support pages:
- Use-case pages for common jobs.
- Troubleshooting pages for failure modes.
- Spec/reference pages only when requirements are maintained.

Required comparison pages:
- Compare adjacent tools with explicit job boundaries.
- Explain when not to use the current tool.
- Link to both alternatives with descriptive anchors.

Internal-link plan:
- Hub to top tools, top support pages, and workflows.
- Tool to hub, adjacent tools, troubleshooting, comparisons, and workflow.
- Support page back to exact tool, hub, and next likely action.
- Cross-cluster links only when the workflow is real.

Schema plan:
- SoftwareApplication for live tools.
- BreadcrumbList for all indexable pages.
- ItemList for hubs and collections.
- FAQ/HowTo only when the visible content actually contains that structure.

Image/media plan:
- Use media only when it clarifies the task or improves preview CTR.
- Descriptive filenames and alt text.
- No decorative image inflation.

Content freshness and maintenance:
- Review query data monthly once Search Console has impressions.
- Review specs quarterly, or sooner when a platform requirement changes.
- Mark pages for rewrite if they lose traffic while the matching tool remains healthy.

## 3. Passport, ID, and application photo workflows

Why this cluster matters:
High intent and repeatable specs, but it must be product-backed because misleading ID/passport pages damage trust.

Expected traffic ceiling:
500K-1.5M monthly views when real crop, background, dimensions, and validation tools support the claims.

Monetization alignment:
High if the on-site result remains useful and the partner link is clearly optional.

Top 20 seed pages:
1. passport-photo-maker
2. white-background-photo-tool
3. id-photo-cropper
4. application-photo-maker
5. visa-photo-resizer
6. 2x2-photo-maker
7. 35x45mm-photo-tool
8. signature-photo-maker
9. document-photo-background
10. photo-for-online-form
11. student-id-photo-tool
12. job-application-photo-tool
13. passport-photo-size-guide
14. id-photo-aspect-ratio
15. photo-background-white-guide
16. crop-face-centered-photo
17. photo-size-requirements
18. printable-passport-photo-sheet
19. mobile-passport-photo-check
20. application-photo-upload-prep

Required tool pages:
- Build or upgrade only the tools that can complete the task in the browser.
- Preserve client-side privacy messaging and clear input/output constraints.
- Add examples, edge cases, and adjacent-tool links to every important tool page.

Required support pages:
- Use-case pages for common jobs.
- Troubleshooting pages for failure modes.
- Spec/reference pages only when requirements are maintained.

Required comparison pages:
- Compare adjacent tools with explicit job boundaries.
- Explain when not to use the current tool.
- Link to both alternatives with descriptive anchors.

Internal-link plan:
- Hub to top tools, top support pages, and workflows.
- Tool to hub, adjacent tools, troubleshooting, comparisons, and workflow.
- Support page back to exact tool, hub, and next likely action.
- Cross-cluster links only when the workflow is real.

Schema plan:
- SoftwareApplication for live tools.
- BreadcrumbList for all indexable pages.
- ItemList for hubs and collections.
- FAQ/HowTo only when the visible content actually contains that structure.

Image/media plan:
- Use media only when it clarifies the task or improves preview CTR.
- Descriptive filenames and alt text.
- No decorative image inflation.

Content freshness and maintenance:
- Review query data monthly once Search Console has impressions.
- Review specs quarterly, or sooner when a platform requirement changes.
- Mark pages for rewrite if they lose traffic while the matching tool remains healthy.

## 4. Metadata, SEO, and publishing launch tools

Why this cluster matters:
Useful for creators and developers, strong internal fit with open-source teaching, but pages need working checkers rather than generic SEO copy.

Expected traffic ceiling:
500K-1.2M monthly views after preview/checker tools exist and can provide original diagnostics.

Monetization alignment:
Medium. Intent is professional and repeat-use, but ad tolerance is lower.

Top 20 seed pages:
1. meta-title-checker
2. meta-description-checker
3. og-preview
4. twitter-card-preview
5. robots-txt-builder
6. sitemap-generator
7. slug-generator
8. canonical-url-checker
9. schema-helper
10. favicon-checker
11. page-title-length-guide
12. meta-description-length-guide
13. open-graph-debug-guide
14. robots-txt-template-guide
15. sitemap-index-guide
16. launch-seo-checklist
17. hreflang-checker
18. social-preview-checker
19. structured-data-validator
20. metadata-cleanup-workflow

Required tool pages:
- Build or upgrade only the tools that can complete the task in the browser.
- Preserve client-side privacy messaging and clear input/output constraints.
- Add examples, edge cases, and adjacent-tool links to every important tool page.

Required support pages:
- Use-case pages for common jobs.
- Troubleshooting pages for failure modes.
- Spec/reference pages only when requirements are maintained.

Required comparison pages:
- Compare adjacent tools with explicit job boundaries.
- Explain when not to use the current tool.
- Link to both alternatives with descriptive anchors.

Internal-link plan:
- Hub to top tools, top support pages, and workflows.
- Tool to hub, adjacent tools, troubleshooting, comparisons, and workflow.
- Support page back to exact tool, hub, and next likely action.
- Cross-cluster links only when the workflow is real.

Schema plan:
- SoftwareApplication for live tools.
- BreadcrumbList for all indexable pages.
- ItemList for hubs and collections.
- FAQ/HowTo only when the visible content actually contains that structure.

Image/media plan:
- Use media only when it clarifies the task or improves preview CTR.
- Descriptive filenames and alt text.
- No decorative image inflation.

Content freshness and maintenance:
- Review query data monthly once Search Console has impressions.
- Review specs quarterly, or sooner when a platform requirement changes.
- Mark pages for rewrite if they lose traffic while the matching tool remains healthy.

## 5. Social formatting and publishing cleanup

Why this cluster matters:
Large query surface, easy browser-side product scope, but many keywords are low value unless the tools solve formatting friction directly.

Expected traffic ceiling:
300K-900K monthly views with platform-aware formatters and visible examples.

Monetization alignment:
Medium. High volume but lower commercial depth; keep sponsor previews subtle.

Top 20 seed pages:
1. instagram-line-breaks
2. instagram-bio-formatter
3. tiktok-caption-cleaner
4. youtube-description-formatter
5. linkedin-post-formatter
6. hashtag-cleanup
7. twitter-thread-splitter
8. caption-character-counter
9. emoji-spacing-cleanup
10. social-text-line-breaks
11. youtube-title-checker
12. pinterest-description-formatter
13. facebook-post-formatter
14. social-link-preview
15. bio-link-text-cleaner
16. caption-to-bullets
17. remove-extra-spaces
18. normalize-smart-quotes
19. utm-caption-cleanup
20. social-publishing-checklist

Required tool pages:
- Build or upgrade only the tools that can complete the task in the browser.
- Preserve client-side privacy messaging and clear input/output constraints.
- Add examples, edge cases, and adjacent-tool links to every important tool page.

Required support pages:
- Use-case pages for common jobs.
- Troubleshooting pages for failure modes.
- Spec/reference pages only when requirements are maintained.

Required comparison pages:
- Compare adjacent tools with explicit job boundaries.
- Explain when not to use the current tool.
- Link to both alternatives with descriptive anchors.

Internal-link plan:
- Hub to top tools, top support pages, and workflows.
- Tool to hub, adjacent tools, troubleshooting, comparisons, and workflow.
- Support page back to exact tool, hub, and next likely action.
- Cross-cluster links only when the workflow is real.

Schema plan:
- SoftwareApplication for live tools.
- BreadcrumbList for all indexable pages.
- ItemList for hubs and collections.
- FAQ/HowTo only when the visible content actually contains that structure.

Image/media plan:
- Use media only when it clarifies the task or improves preview CTR.
- Descriptive filenames and alt text.
- No decorative image inflation.

Content freshness and maintenance:
- Review query data monthly once Search Console has impressions.
- Review specs quarterly, or sooner when a platform requirement changes.
- Mark pages for rewrite if they lose traffic while the matching tool remains healthy.

## 6. Evergreen calculators and light developer utilities

Why this cluster matters:
Big market, but crowded. Build selectively around repeat use, privacy, and workflow grouping.

Expected traffic ceiling:
1M-2M monthly views only after repeat-use quality beats commodity calculators and formatters.

Monetization alignment:
Medium. Repeat visits can be valuable, but pages must stay fast and uncluttered.

Top 20 seed pages:
1. age-calculator
2. date-difference-calculator
3. percentage-calculator
4. word-counter
5. character-counter
6. unit-converter
7. json-formatter
8. base64-decode
9. base64-encode
10. url-encode
11. url-decode
12. uuid-generator
13. hash-generator
14. timestamp-converter
15. color-converter
16. regex-tester
17. csv-to-json
18. json-to-csv
19. markdown-preview
20. text-diff-checker

Required tool pages:
- Build or upgrade only the tools that can complete the task in the browser.
- Preserve client-side privacy messaging and clear input/output constraints.
- Add examples, edge cases, and adjacent-tool links to every important tool page.

Required support pages:
- Use-case pages for common jobs.
- Troubleshooting pages for failure modes.
- Spec/reference pages only when requirements are maintained.

Required comparison pages:
- Compare adjacent tools with explicit job boundaries.
- Explain when not to use the current tool.
- Link to both alternatives with descriptive anchors.

Internal-link plan:
- Hub to top tools, top support pages, and workflows.
- Tool to hub, adjacent tools, troubleshooting, comparisons, and workflow.
- Support page back to exact tool, hub, and next likely action.
- Cross-cluster links only when the workflow is real.

Schema plan:
- SoftwareApplication for live tools.
- BreadcrumbList for all indexable pages.
- ItemList for hubs and collections.
- FAQ/HowTo only when the visible content actually contains that structure.

Image/media plan:
- Use media only when it clarifies the task or improves preview CTR.
- Descriptive filenames and alt text.
- No decorative image inflation.

Content freshness and maintenance:
- Review query data monthly once Search Console has impressions.
- Review specs quarterly, or sooner when a platform requirement changes.
- Mark pages for rewrite if they lose traffic while the matching tool remains healthy.
